# disable titlebar mouse scroll
bindsym button4 nop
bindsym button5 nop

# swipe gestrue
bindgesture swipe:right focus next
bindgesture swipe:left focus prev

bindgesture swipe:up workspace prev
bindgesture swipe:down workspace next

# swithch to workspace number
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8

# swaylock
bindsym $mod+Shift+x exec $lockscreen

# window operations
floating_modifier $mod

bindsym $mod+a splitv
bindsym $mod+Shift+a splith

bindsym $mod+Shift+q kill
bindsym $mod+Shift+r reload

bindsym $mod+m fullscreen toggle

bindsym $mod+Shift+o layout toggle tabbed splitv splith

# floating windows
bindsym $mod+space floating toggle; [floating con_id="__focused__"] move position center; [floating con_id="__focused__"] resize set 70ppt 75ppt
bindsym $mod+Shift+space [floating con_id="__focused__"] move position center;[floating con_id="__focused__"] resize set 70ppt 75ppt
bindsym $mod+Shift+y [floating con_id="__focused__"] move position center

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

bindsym $mod+u focus parent
bindsym $mod+i focus child

bindsym $mod+Shift+n focus next
bindsym $mod+Shift+p focus prev

bindsym $mod+q focus mode_toggle

# toggle between workspaces
bindsym $mod+Tab workspace next_on_output
bindsym Mod1+Tab workspace prev_on_output

# move windows
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# resize window (you can also use the mouse for that):
bindsym $mod+Ctrl+h resize grow   width  20 px or 5 ppt
bindsym $mod+Ctrl+j resize shrink height 20 px or 5 ppt
bindsym $mod+Ctrl+k resize grow   height 20 px or 5 ppt
bindsym $mod+Ctrl+l resize shrink width  20 px or 5 ppt

# screenshot
bindsym Print exec $full_screenshot
bindsym $mod+Print exec $edit_screenshot

# modes : for imitating keychords
mode "swap" {
  bindsym $mod+Shift+h mark --add "_swap", focus left,  swap container with mark "_swap", focus left , unmark "_swap" ; mode default
  bindsym $mod+Shift+j mark --add "_swap", focus down,  swap container with mark "_swap", focus down , unmark "_swap" ; mode default
  bindsym $mod+Shift+k mark --add "_swap", focus up,    swap container with mark "_swap", focus up   , unmark "_swap" ; mode default
  bindsym $mod+Shift+l mark --add "_swap", focus right, swap container with mark "_swap", focus right, unmark "_swap" ; mode default

  # escape mode keys
  bindsym Escape mode "default"
  bindsym Super_L mode "default"
  bindsym Return mode "default"
  bindsym Space mode "default"
}

mode "launch" {
  bindsym $mod+Return exec $terminal ; mode "default"
  bindsym $mod+w exec $browser ; mode "default"
  bindsym $mod+i exec $incognito ; mode "default"
  bindsym $mod+n exec $filemanager ; mode "default"
  bindsym $mod+o exec $pdfreader ; mode "default"
  bindsym $mod+b exec $blueman ; mode "default"
  bindsym $mod+p exec $audio_settings ; mode "default"
  bindsym $mod+m exec $musicplayer ; mode "default"
  bindsym $mod+u exec $backgrounds ; mode "default"

  # escape mode keys
  bindsym Escape mode "default"
  bindsym Super_L mode "default"
  bindsym Return mode "default"
  bindsym Space mode "default"
}

mode "webapps" {
  bindsym y exec $youtube ; mode "default"
  bindsym a exec $chatgpt ; mode "default"
  bindsym i exec $chatgptPrivate ; mode "default"
  bindsym g exec $github ; mode "default"
  bindsym r exec $reddit ; mode "default"
  bindsym f exec $facebook ; mode "default"
  bindsym m exec $messenger ; mode "default"
  bindsym t exec $monkeytype ; mode "default"

  # escape mode keys
  bindsym Escape mode "default"
  bindsym Super_L mode "default"
  bindsym Return mode "default"
  bindsym Space mode "default"
}

mode "rofi" {
  bindsym $mod+d exec $rofi_drun ; mode "default"
  bindsym $mod+Shift+d exec $rofi_run ; mode "default"
  bindsym $mod+w exec $rofi_windows ; mode "default"
  bindsym $mod+e exec $rofi_emoji; mode "default"
  bindsym $mod+Shift+e exec $powermenu ; mode "default"

  # escape mode keys
  bindsym Escape mode "default"
  bindsym Super_L mode "default"
  bindsym Return mode "default"
  bindsym Space mode "default"
}

mode "notify" {
  bindsym m exec makoctl restore
  bindsym Shift+m exec makoctl dismiss --all ; mode "default"

  # escape mode keys
  bindsym Escape mode "default"
  bindsym Super_L mode "default"
  bindsym Return mode "default"
  bindsym Space mode "default"
}

mode "script" {
  bindsym b exec $waybarThemeSwitcher ; mode "default"
  bindsym t exec $wlsunset ; mode "default"
  bindsym w exec $chooseBackground ; mode "default"

  # escape mode keys
  bindsym Escape mode "default"
  bindsym Super_L mode "default"
  bindsym Return mode "default"
  bindsym Space mode "default"
}

# trigger mode keymaps
bindsym $mod+d mode "rofi"
bindsym $mod+return mode "launch"
bindsym $mod+Shift+w mode "webapps"
bindsym $mod+Shift+u mode "swap"
bindsym $mod+Shift+s mode "script"
bindsym $mod+Shift+m mode "notify"

# brightness
bindsym XF86MonBrightnessUp exec brightnessctl set +5%
bindsym XF86MonBrightnessDown exec brightnessctl --min-value=1 set 5%-

bindsym $mod+F4 exec brightnessctl set +5%
bindsym $mod+F3 exec brightnessctl --min-value=1 set 5%-

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume 0 +5% 
bindsym XF86AudioLowerVolume exec pactl set-sink-volume 0 -5% 
bindsym XF86AudioMute exec pactl set-sink-mute 0 toggle

bindsym $mod+F7 exec pactl set-sink-volume 0 +5% 
bindsym $mod+F6 exec pactl set-sink-volume 0 -5% 
bindsym $mod+F5 exec pactl set-sink-mute 0 toggle

# Media player controls
bindsym $mod+Shift+up exec playerctl play
bindsym $mod+Shift+down exec playerctl pause
bindsym $mod+Shift+right exec playerctl next       
bindsym $mod+Shift+left exec playerctl previous   

bindsym XF86AudioPlay exec playerctl play
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous
